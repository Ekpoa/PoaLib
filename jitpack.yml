jdk:
  - openjdk21

before_install:
  - java -version
  - mvn -version
  - sudo apt-get update && sudo apt-get install -y maven
  - mvn --version
  - mvn paper-nms:init -e -X

install:
  - mvn clean install -DskipTests