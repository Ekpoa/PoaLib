jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.3-amzn
  - sdk use java 17.0.3-amzn
  - mvn package dependency:resolve
  - mvn paper-nms:init

install:
  - mvn clean install -DskipTests